const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const users = await prisma.user.findMany(); console.log('Users in public.User:', users); const authUsers = await prisma.$queryRawUnsafe("SELECT id, email FROM auth.users"); console.log('Users in auth.users:', authUsers); } main().catch(console.error).finally(() => prisma.$disconnect());
