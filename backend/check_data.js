const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { console.log('Users:', await prisma.user.count()); console.log('Bookings:', await prisma.booking.count()); console.log('Routes:', await prisma.route.count()); console.log('Trips:', await prisma.trip.count()); console.log('Schedules:', await prisma.tripSchedule.count()); } main().catch(console.error).finally(() => prisma.$disconnect());
