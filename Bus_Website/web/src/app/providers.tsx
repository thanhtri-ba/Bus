import type { ReactNode } from 'react'; import { QueryClientProvider } from '@tanstack/react-query'; import { ReactQueryDevtools } from '@tanstack/react-query-devtools'; import { ErrorBoundary } from 'react-error-boundary'; import { RouterProvider } from 'react-router-dom'; import { Toaster } from 'sonner'; import { queryClient } from '@/app/queryClient'; import { router } from '@/app/router'; import { ErrorState } from '@/shared/components/states/ErrorState';
export function AppProviders({children}:{children?:ReactNode}){return <ErrorBoundary fallbackRender={({resetErrorBoundary})=><ErrorState description="Ứng dụng gặp lỗi ngoài dự kiến." onRetry={resetErrorBoundary}/>}><QueryClientProvider client={queryClient}>{children??<RouterProvider router={router}/>}<Toaster richColors position="top-right"/>{import.meta.env.DEV&&<ReactQueryDevtools initialIsOpen={false}/>}</QueryClientProvider></ErrorBoundary>}
